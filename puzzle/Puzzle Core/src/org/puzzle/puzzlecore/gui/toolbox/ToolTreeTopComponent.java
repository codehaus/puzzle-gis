/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */

package org.puzzle.puzzlecore.gui.toolbox;

import java.awt.BorderLayout;
import java.io.Serializable;
import java.util.logging.Logger;
import org.geotools.gui.swing.toolbox.tooltree.JToolTree;
import org.openide.util.NbBundle;
import org.openide.windows.TopComponent;
import org.openide.windows.WindowManager;
import org.openide.util.Utilities;
import org.puzzle.puzzlecore.struct.CORE;
import org.puzzle.puzzlecore.struct.ToolManager;

/**
 * Top component which displays something.
 */
final class ToolTreeTopComponent extends TopComponent {

    private JToolTree tree = null;
    
    
    private static ToolTreeTopComponent instance;
    /** path to the icon used by the component and its open action */
    static final String ICON_PATH = "org/puzzle/puzzlecore/gui/toolbox/tool.png";

    private static final String PREFERRED_ID = "ToolTreeTopComponent";

    private ToolTreeTopComponent() {
        initComponents();
        setName(NbBundle.getMessage(ToolTreeTopComponent.class, "CTL_ToolTreeTopComponent"));
        setToolTipText(NbBundle.getMessage(ToolTreeTopComponent.class, "HINT_ToolTreeTopComponent"));
        setIcon(Utilities.loadImage(ICON_PATH, true));
    }

    /** This method is called from within the constructor to
     * initialize the form.
     * WARNING: Do NOT modify this code. The content of this method is
     * always regenerated by the Form Editor.
     */
    // <editor-fold defaultstate="collapsed" desc="Generated Code">//GEN-BEGIN:initComponents
    private void initComponents() {

        setLayout(new java.awt.BorderLayout());
    }// </editor-fold>//GEN-END:initComponents
    // Variables declaration - do not modify//GEN-BEGIN:variables
    // End of variables declaration//GEN-END:variables
    /**
     * Gets default instance. Do not use directly: reserved for *.settings files only,
     * i.e. deserialization routines; otherwise you could get a non-deserialized instance.
     * To obtain the singleton instance, use {@link findInstance}.
     */
    public static synchronized ToolTreeTopComponent getDefault() {
        if (instance == null) {
            instance = new ToolTreeTopComponent();
        }
        return instance;
    }

    /**
     * Obtain the ToolTreeTopComponent instance. Never call {@link #getDefault} directly!
     */
    public static synchronized ToolTreeTopComponent findInstance() {
        TopComponent win = WindowManager.getDefault().findTopComponent(PREFERRED_ID);
        if (win == null) {
            Logger.getLogger(ToolTreeTopComponent.class.getName()).warning(
                    "Cannot find " + PREFERRED_ID + " component. It will not be located properly in the window system.");
            return getDefault();
        }
        if (win instanceof ToolTreeTopComponent) {
            return (ToolTreeTopComponent) win;
        }
        Logger.getLogger(ToolTreeTopComponent.class.getName()).warning(
                "There seem to be multiple components with the '" + PREFERRED_ID +
                "' ID. That is a potential source of errors and unexpected behavior.");
        return getDefault();
    }

    @Override
    public int getPersistenceType() {
        return TopComponent.PERSISTENCE_NEVER;
    }

    @Override
    public void componentOpened() {
        removeAll();
        if(tree == null){
            tree = CORE.getToolManager().getToolTree();
        }
        
        add(BorderLayout.CENTER,tree);
    }

    @Override
    public void componentClosed() {
        removeAll();        
        tree = null;
    }

    /** replaces this in object stream */
    @Override
    public Object writeReplace() {
        return new ResolvableHelper();
    }

    @Override
    protected String preferredID() {
        return PREFERRED_ID;
    }

    final static class ResolvableHelper implements Serializable {

        private static final long serialVersionUID = 1L;

        public Object readResolve() {
            return ToolTreeTopComponent.getDefault();
        }
    }
}
